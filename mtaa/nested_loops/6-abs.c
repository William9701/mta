#include <stdio.h>
#include "main.h"

int _abs(int n)
{
	if (n == 0)
	{
		printf("%d", n);
		return 0;
	}
	if (n > 0)
	{
			printf("%d", n);
		return 0;
	}
	if (n < 0)
	{
		printf("%d" / -1, n);
	}
	return 0;
}
